# ET Office Desktop — daemon dependencies
# เวอร์ชัน lock ไว้ทั้งหมด — โดยเฉพาะ CrewAI ที่มี breaking changes บ่อย (M0-3)
# ทดสอบบน Python 3.12.10 / Windows 10

crewai==1.14.6
fastapi==0.136.3
uvicorn==0.49.0
sqlalchemy==2.0.50
python-dotenv==1.2.2
pyyaml==6.0.3
python-multipart==0.0.32
pystray==0.19.5
websockets==16.0

# หมายเหตุ: chroma-hnswlib (transitive dep ของ crewai) ไม่มี source build บน Windows
# ถ้า pip พยายาม build จาก source ให้ติดตั้งด้วย: pip install --prefer-binary -r requirements.txt
pywebview==6.2.1   # sidebar host (M4-1)
